import { ComingSoon } from '~/components/ui'

export default function FocusScreen() {
  return <ComingSoon title="Focus" note="The timer is ported in the core phase, with background-safe timing." />
}
